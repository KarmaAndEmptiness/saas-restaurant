/**
 *
 *  PermissionCategory.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "PermissionCategory.h"
#include "Permission.h"
#include "Tenant.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::saas_restaurant;

const std::string PermissionCategory::Cols::_category_id = "category_id";
const std::string PermissionCategory::Cols::_tenant_id = "tenant_id";
const std::string PermissionCategory::Cols::_category_name = "category_name";
const std::string PermissionCategory::primaryKeyName = "category_id";
const bool PermissionCategory::hasPrimaryKey = true;
const std::string PermissionCategory::tableName = "permission_category";

const std::vector<typename PermissionCategory::MetaData> PermissionCategory::metaData_={
{"category_id","uint32_t","int(10) unsigned",4,1,1,1},
{"tenant_id","uint32_t","int(10) unsigned",4,0,0,0},
{"category_name","std::string","varchar(255)",255,0,0,0}
};
const std::string &PermissionCategory::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
PermissionCategory::PermissionCategory(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
        if(!r["category_id"].isNull())
        {
            categoryId_=std::make_shared<uint32_t>(r["category_id"].as<uint32_t>());
        }
        if(!r["tenant_id"].isNull())
        {
            tenantId_=std::make_shared<uint32_t>(r["tenant_id"].as<uint32_t>());
        }
        if(!r["category_name"].isNull())
        {
            categoryName_=std::make_shared<std::string>(r["category_name"].as<std::string>());
        }
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 3 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
        index = offset + 0;
        if(!r[index].isNull())
        {
            categoryId_=std::make_shared<uint32_t>(r[index].as<uint32_t>());
        }
        index = offset + 1;
        if(!r[index].isNull())
        {
            tenantId_=std::make_shared<uint32_t>(r[index].as<uint32_t>());
        }
        index = offset + 2;
        if(!r[index].isNull())
        {
            categoryName_=std::make_shared<std::string>(r[index].as<std::string>());
        }
    }

}

PermissionCategory::PermissionCategory(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 3)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        dirtyFlag_[0] = true;
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            categoryId_=std::make_shared<uint32_t>((uint32_t)pJson[pMasqueradingVector[0]].asUInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            tenantId_=std::make_shared<uint32_t>((uint32_t)pJson[pMasqueradingVector[1]].asUInt64());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            categoryName_=std::make_shared<std::string>(pJson[pMasqueradingVector[2]].asString());
        }
    }
}

PermissionCategory::PermissionCategory(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("category_id"))
    {
        dirtyFlag_[0]=true;
        if(!pJson["category_id"].isNull())
        {
            categoryId_=std::make_shared<uint32_t>((uint32_t)pJson["category_id"].asUInt64());
        }
    }
    if(pJson.isMember("tenant_id"))
    {
        dirtyFlag_[1]=true;
        if(!pJson["tenant_id"].isNull())
        {
            tenantId_=std::make_shared<uint32_t>((uint32_t)pJson["tenant_id"].asUInt64());
        }
    }
    if(pJson.isMember("category_name"))
    {
        dirtyFlag_[2]=true;
        if(!pJson["category_name"].isNull())
        {
            categoryName_=std::make_shared<std::string>(pJson["category_name"].asString());
        }
    }
}

void PermissionCategory::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 3)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            categoryId_=std::make_shared<uint32_t>((uint32_t)pJson[pMasqueradingVector[0]].asUInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            tenantId_=std::make_shared<uint32_t>((uint32_t)pJson[pMasqueradingVector[1]].asUInt64());
        }
    }
    if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
    {
        dirtyFlag_[2] = true;
        if(!pJson[pMasqueradingVector[2]].isNull())
        {
            categoryName_=std::make_shared<std::string>(pJson[pMasqueradingVector[2]].asString());
        }
    }
}

void PermissionCategory::updateByJson(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("category_id"))
    {
        if(!pJson["category_id"].isNull())
        {
            categoryId_=std::make_shared<uint32_t>((uint32_t)pJson["category_id"].asUInt64());
        }
    }
    if(pJson.isMember("tenant_id"))
    {
        dirtyFlag_[1] = true;
        if(!pJson["tenant_id"].isNull())
        {
            tenantId_=std::make_shared<uint32_t>((uint32_t)pJson["tenant_id"].asUInt64());
        }
    }
    if(pJson.isMember("category_name"))
    {
        dirtyFlag_[2] = true;
        if(!pJson["category_name"].isNull())
        {
            categoryName_=std::make_shared<std::string>(pJson["category_name"].asString());
        }
    }
}

const uint32_t &PermissionCategory::getValueOfCategoryId() const noexcept
{
    static const uint32_t defaultValue = uint32_t();
    if(categoryId_)
        return *categoryId_;
    return defaultValue;
}
const std::shared_ptr<uint32_t> &PermissionCategory::getCategoryId() const noexcept
{
    return categoryId_;
}
void PermissionCategory::setCategoryId(const uint32_t &pCategoryId) noexcept
{
    categoryId_ = std::make_shared<uint32_t>(pCategoryId);
    dirtyFlag_[0] = true;
}
const typename PermissionCategory::PrimaryKeyType & PermissionCategory::getPrimaryKey() const
{
    assert(categoryId_);
    return *categoryId_;
}

const uint32_t &PermissionCategory::getValueOfTenantId() const noexcept
{
    static const uint32_t defaultValue = uint32_t();
    if(tenantId_)
        return *tenantId_;
    return defaultValue;
}
const std::shared_ptr<uint32_t> &PermissionCategory::getTenantId() const noexcept
{
    return tenantId_;
}
void PermissionCategory::setTenantId(const uint32_t &pTenantId) noexcept
{
    tenantId_ = std::make_shared<uint32_t>(pTenantId);
    dirtyFlag_[1] = true;
}
void PermissionCategory::setTenantIdToNull() noexcept
{
    tenantId_.reset();
    dirtyFlag_[1] = true;
}

const std::string &PermissionCategory::getValueOfCategoryName() const noexcept
{
    static const std::string defaultValue = std::string();
    if(categoryName_)
        return *categoryName_;
    return defaultValue;
}
const std::shared_ptr<std::string> &PermissionCategory::getCategoryName() const noexcept
{
    return categoryName_;
}
void PermissionCategory::setCategoryName(const std::string &pCategoryName) noexcept
{
    categoryName_ = std::make_shared<std::string>(pCategoryName);
    dirtyFlag_[2] = true;
}
void PermissionCategory::setCategoryName(std::string &&pCategoryName) noexcept
{
    categoryName_ = std::make_shared<std::string>(std::move(pCategoryName));
    dirtyFlag_[2] = true;
}
void PermissionCategory::setCategoryNameToNull() noexcept
{
    categoryName_.reset();
    dirtyFlag_[2] = true;
}

void PermissionCategory::updateId(const uint64_t id)
{
    categoryId_ = std::make_shared<uint32_t>(static_cast<uint32_t>(id));
}

const std::vector<std::string> &PermissionCategory::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
        "tenant_id",
        "category_name"
    };
    return inCols;
}

void PermissionCategory::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getTenantId())
        {
            binder << getValueOfTenantId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getCategoryName())
        {
            binder << getValueOfCategoryName();
        }
        else
        {
            binder << nullptr;
        }
    }
}

const std::vector<std::string> PermissionCategory::updateColumns() const
{
    std::vector<std::string> ret;
    if(dirtyFlag_[1])
    {
        ret.push_back(getColumnName(1));
    }
    if(dirtyFlag_[2])
    {
        ret.push_back(getColumnName(2));
    }
    return ret;
}

void PermissionCategory::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[1])
    {
        if(getTenantId())
        {
            binder << getValueOfTenantId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[2])
    {
        if(getCategoryName())
        {
            binder << getValueOfCategoryName();
        }
        else
        {
            binder << nullptr;
        }
    }
}
Json::Value PermissionCategory::toJson() const
{
    Json::Value ret;
    if(getCategoryId())
    {
        ret["category_id"]=getValueOfCategoryId();
    }
    else
    {
        ret["category_id"]=Json::Value();
    }
    if(getTenantId())
    {
        ret["tenant_id"]=getValueOfTenantId();
    }
    else
    {
        ret["tenant_id"]=Json::Value();
    }
    if(getCategoryName())
    {
        ret["category_name"]=getValueOfCategoryName();
    }
    else
    {
        ret["category_name"]=Json::Value();
    }
    return ret;
}

Json::Value PermissionCategory::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 3)
    {
        if(!pMasqueradingVector[0].empty())
        {
            if(getCategoryId())
            {
                ret[pMasqueradingVector[0]]=getValueOfCategoryId();
            }
            else
            {
                ret[pMasqueradingVector[0]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[1].empty())
        {
            if(getTenantId())
            {
                ret[pMasqueradingVector[1]]=getValueOfTenantId();
            }
            else
            {
                ret[pMasqueradingVector[1]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[2].empty())
        {
            if(getCategoryName())
            {
                ret[pMasqueradingVector[2]]=getValueOfCategoryName();
            }
            else
            {
                ret[pMasqueradingVector[2]]=Json::Value();
            }
        }
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    if(getCategoryId())
    {
        ret["category_id"]=getValueOfCategoryId();
    }
    else
    {
        ret["category_id"]=Json::Value();
    }
    if(getTenantId())
    {
        ret["tenant_id"]=getValueOfTenantId();
    }
    else
    {
        ret["tenant_id"]=Json::Value();
    }
    if(getCategoryName())
    {
        ret["category_name"]=getValueOfCategoryName();
    }
    else
    {
        ret["category_name"]=Json::Value();
    }
    return ret;
}

bool PermissionCategory::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("category_id"))
    {
        if(!validJsonOfField(0, "category_id", pJson["category_id"], err, true))
            return false;
    }
    if(pJson.isMember("tenant_id"))
    {
        if(!validJsonOfField(1, "tenant_id", pJson["tenant_id"], err, true))
            return false;
    }
    if(pJson.isMember("category_name"))
    {
        if(!validJsonOfField(2, "category_name", pJson["category_name"], err, true))
            return false;
    }
    return true;
}
bool PermissionCategory::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                            const std::vector<std::string> &pMasqueradingVector,
                                                            std::string &err)
{
    if(pMasqueradingVector.size() != 3)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty())
      {
          if(pJson.isMember(pMasqueradingVector[0]))
          {
              if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[1].empty())
      {
          if(pJson.isMember(pMasqueradingVector[1]))
          {
              if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, true))
                  return false;
          }
      }
      if(!pMasqueradingVector[2].empty())
      {
          if(pJson.isMember(pMasqueradingVector[2]))
          {
              if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, true))
                  return false;
          }
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool PermissionCategory::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("category_id"))
    {
        if(!validJsonOfField(0, "category_id", pJson["category_id"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    if(pJson.isMember("tenant_id"))
    {
        if(!validJsonOfField(1, "tenant_id", pJson["tenant_id"], err, false))
            return false;
    }
    if(pJson.isMember("category_name"))
    {
        if(!validJsonOfField(2, "category_name", pJson["category_name"], err, false))
            return false;
    }
    return true;
}
bool PermissionCategory::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                                          const std::vector<std::string> &pMasqueradingVector,
                                                          std::string &err)
{
    if(pMasqueradingVector.size() != 3)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
      {
          if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
      if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
      {
          if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, false))
              return false;
      }
      if(!pMasqueradingVector[2].empty() && pJson.isMember(pMasqueradingVector[2]))
      {
          if(!validJsonOfField(2, pMasqueradingVector[2], pJson[pMasqueradingVector[2]], err, false))
              return false;
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool PermissionCategory::validJsonOfField(size_t index,
                                          const std::string &fieldName,
                                          const Json::Value &pJson,
                                          std::string &err,
                                          bool isForCreation)
{
    switch(index)
    {
        case 0:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(isForCreation)
            {
                err="The automatic primary key cannot be set";
                return false;
            }
            if(!pJson.isUInt())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 1:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isUInt())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 2:
            if(pJson.isNull())
            {
                return true;
            }
            if(!pJson.isString())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            if(pJson.isString() && std::strlen(pJson.asCString()) > 255)
            {
                err="String length exceeds limit for the " +
                    fieldName +
                    " field (the maximum value is 255)";
                return false;
            }

            break;
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
Tenant PermissionCategory::getTenant(const DbClientPtr &clientPtr) const {
    static const std::string sql = "select * from tenant where tenant_id = ?";
    Result r(nullptr);
    {
        auto binder = *clientPtr << sql;
        binder << *tenantId_ << Mode::Blocking >>
            [&r](const Result &result) { r = result; };
        binder.exec();
    }
    if (r.size() == 0)
    {
        throw UnexpectedRows("0 rows found");
    }
    else if (r.size() > 1)
    {
        throw UnexpectedRows("Found more than one row");
    }
    return Tenant(r[0]);
}

void PermissionCategory::getTenant(const DbClientPtr &clientPtr,
                                   const std::function<void(Tenant)> &rcb,
                                   const ExceptionCallback &ecb) const
{
    static const std::string sql = "select * from tenant where tenant_id = ?";
    *clientPtr << sql
               << *tenantId_
               >> [rcb = std::move(rcb), ecb](const Result &r){
                    if (r.size() == 0)
                    {
                        ecb(UnexpectedRows("0 rows found"));
                    }
                    else if (r.size() > 1)
                    {
                        ecb(UnexpectedRows("Found more than one row"));
                    }
                    else
                    {
                        rcb(Tenant(r[0]));
                    }
               }
               >> ecb;
}
std::vector<Permission> PermissionCategory::getPermissions(const DbClientPtr &clientPtr) const {
    static const std::string sql = "select * from permission where permission_category_id = ?";
    Result r(nullptr);
    {
        auto binder = *clientPtr << sql;
        binder << *categoryId_ << Mode::Blocking >>
            [&r](const Result &result) { r = result; };
        binder.exec();
    }
    std::vector<Permission> ret;
    ret.reserve(r.size());
    for (auto const &row : r)
    {
        ret.emplace_back(Permission(row));
    }
    return ret;
}

void PermissionCategory::getPermissions(const DbClientPtr &clientPtr,
                                        const std::function<void(std::vector<Permission>)> &rcb,
                                        const ExceptionCallback &ecb) const
{
    static const std::string sql = "select * from permission where permission_category_id = ?";
    *clientPtr << sql
               << *categoryId_
               >> [rcb = std::move(rcb)](const Result &r){
                   std::vector<Permission> ret;
                   ret.reserve(r.size());
                   for (auto const &row : r)
                   {
                       ret.emplace_back(Permission(row));
                   }
                   rcb(ret);
               }
               >> ecb;
}
